# Recommended environment variable

Add this to your local `.env` when using the shared workflow architecture:

```env
WORKSPACE_ROOT=/workspace
```

Examples:
```env
WORKSPACE_ROOT=/Users/you/workspace
```

This is the canonical local root that contains:
- `workflow/`
- project folders
